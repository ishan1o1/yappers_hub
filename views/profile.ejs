<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yapper's Hub</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 box-border">
    <div class="w-full h-screen bg-zinc-900 p-10 text-white ">
        <div class="relative w-full">
            <h1 class="text-5xl text-center">Yapper's Hub</h1>
            <a href="/logout" class="absolute top-0 right-0 px-5 py-2 mb-8 border-2 border-zinc-800 rounded-lg bg-red-500">logout</a>
        </div>
        
        
        
        
        
        <h1 class="text-3xl tracking-tighter text-blue mb-3"> Dashboard</h1>
        <div class="w-full min h-screen bg-zinc-900 p-10 text-white ">
            <div class="flex itmes-start gap-4">

                <div class="w-10 h-10 bg-red-500 rounded-md"> </div>
            <h3 class="text-3xl tracking-tighter text-blue mb-3"><span class="font-light">Hello</span>,<%= user.name %></h3>
            </div>
            
            <h5 class="text-2xl tracking-tighter text-blue mb-3">You can create a new post.</h5>
<form action="/post" method="POST">
    <textarea class="resize-none bg-transparent border-2 broder-zinc-800 rounded-md w-1/3 p-3 outline-none" name="content" id="" placeholder="Whats on your mind"></textarea>
    <input class="px-3 py-2 bg-blue-500 block rounded-md mt-3 w-40" type="submit" value="Create Post">
</form>
<div class="posts mt-20">
    <h3 class="text-zinc-400">Your Posts</h3>
    <div class="flex gap-2 flex-wrap rounded-md mt-5">
        <% user.posts.reverse().forEach (function (post) { %>
       
        <div class="post w-1/6 mb-3 rounded-md p-3 bg-zinc-700 border-[1px] border-zinc-800">
            <h4 class="text-green-500 mb-2">@<%= user.username %></h4>
            <p class="text-sm tracking-tigh"><%= post.content %></p>
            <small class="mt-1" ><%= post.likes.length %> likes</small>
            <div class="btns flex gap-4 mt-5">
                <a class="text-blue-500" href="/like/<%= post._id %>">
                   like </a>
                <a  class="text-zinc-300" href="/edit/<%= post._id %>">Edit</a>
            </div>
        </div>
        <% }) %>
    </div>
 
</div>
        </div>
       
       
    </div>
    <script src="/javascripts/script.js"></script>
</body>
</html>