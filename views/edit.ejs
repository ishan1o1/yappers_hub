<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 p-10 text-white ">
        <div class="relative w-full mb-8">
            <h1 class="text-5xl text-center">Yapper's Hub</h1>
            <a href="/profile" class="absolute top-0 left-0 px-5 py-2 mb-8 border-2 border-zinc-800 rounded-lg bg-red-250">Go to Dashboard</a>
        </div>
        <h5 class=" mb-5  items-center"> Edit your post</h5>
<form action="/update/<%= post._id %>" method="POST">
    <textarea class="resize-none bg-transparent border-2 broder-zinc-800 rounded-md w-1/3 p-3 outline-none" name="content" id=""><%= post.content %></textarea>
    <input class="px-3 py-2 bg-yellow-500 block text-black rounded-md mt-3 w-40" type="submit" value="Update Post">
</form>
    </div>
</body>
</html>